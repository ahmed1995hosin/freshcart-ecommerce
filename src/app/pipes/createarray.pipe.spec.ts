import { CreatearrayPipe } from './createarray.pipe';

describe('CreatearrayPipe', () => {
  it('create an instance', () => {
    const pipe = new CreatearrayPipe();
    expect(pipe).toBeTruthy();
  });
});
